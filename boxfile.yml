run.config:
  image: nanobox/build
  engine: golang
  engine.config:
    runtime: go-1.8
    package: github.com/nanobox-io/nanobox-adaptor-googlecompute

web.main:
  start: "./nanobox-adaptor-googlecompute"