package main

import (
	"fmt"
	
	"github.com/nanobox-io/nanobox-provider-golang"
)

func (gc GoogleCompute) RenameServer(creds provider.Credentials, id string) error {
	return fmt.Errorf("not supported")
}